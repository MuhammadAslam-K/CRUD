{{>header}}

<section class="vh-99 gradient-custom ">
    {{#unless admin}}
    <form action="/admin_login" method="get" class="d-grid d-md-flex justify-content-md-end">
        <button class="btn btn-outline-light btn-lg px-3">admin </button>
    </form>
    {{/unless}}
    <div class="container py-5 h-100 ">
        <div class="row d-flex justify-content-center align-items-center h-100 ">
            <div class="col-12 col-md-8 col-lg-6 col-xl-5 ">
                <div class="card bg-dark text-white text-dark rounded">
                    <div class="card-body p-5 text-center">
                        {{#if admin}}
                        <form action="/admin_login" method="post">
                            {{else}}
                            <form action="/user_login" method="post">
                                {{/if}}
                                <div class="mb-md-5 mt-md-4 pb-5">

                                    <h2 class="fw-bold mb-2 text-uppercase">Login</h2>
                                    <p class="text-white-50 mb-5">Please enter your login and password!</p>

                                    <div class="form-outline form-white mb-4">
                                        <input type="email" name="email" id="loginEmail"
                                            class="form-control form-control-lg" placeholder="Email" />
                                    </div>

                                    <div class="form-outline form-white mb-4">
                                        <input type="password" name="password" id="loginPassword"
                                            class="form-control form-control-lg" placeholder="Password" />
                                        <span id="loginPassworError" style="color: red;"></span>
                                    </div>

                                    <button onclick="return validation()" class="btn btn-outline-light btn-lg px-5"
                                        type="submit" id="submit">Login</button>

                                </div>
                                {{#unless admin}}
                                <div>
                                    <p class="mb-0">Don't have an account? <a href="/user_signup"
                                            class="text-white-50 fw-bold">Sign
                                            Up</a>
                                    </p>
                                </div>
                                {{/unless}}
                            </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>



{{>footer}}